ALTER TABLE public.events ADD COLUMN IF NOT EXISTS image_urls text[] NOT NULL DEFAULT '{}';
UPDATE public.events SET image_urls = ARRAY[image_url] WHERE image_url IS NOT NULL AND (array_length(image_urls, 1) IS NULL);